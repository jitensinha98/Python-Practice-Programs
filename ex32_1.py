num=int(raw_input("Enter the limit of numbers you want to store :"))
storage=[]
for i in range(0,num):
    print "Storing %d on the list."%i
    storage.append(i)
    
for elements in storage:
    print "Element is = %d"%elements
    
    
    
 
